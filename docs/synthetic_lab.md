# Synthetic Lab

The synthetic lab creates fictional records with configurable student count, weekly activity period, and random seed. Default outputs are written to `outputs/` and are ignored by Git.

The generated data is intentionally fictional and includes pseudonymized outputs for downstream analysis. Do not replace it with real student data unless a full privacy and governance process is in place.
